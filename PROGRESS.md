Ostatnio ukończone zadanie: **EPIC 11 Sprint 11.1 — śledzenie followersów/subskrybentów** (PR #82). Użytkownik poprosił o wykonanie Sprint 11.1 i 11.2 naraz; 11.1 zbudowane w całości (zero nowych zgód OAuth potrzebnych), 11.2 (komentarze/DM) świadomie odłożone — jego blokery to akcje na zewnętrznych kontach dewelopera (Meta App Dashboard, Google Cloud Console, weryfikacja u TikToka), do których appka nie ma dostępu, wyjaśnione wprost zamiast budowania kodu pod niepotwierdzone dane. Nowy model `AccountGrowthSnapshot` (prawdziwa seria czasowa, nie upsert-latest-only jak `PostMetric` - bo "wzrost" wymaga historii). Endpoint TikToka zweryfikowany dokładnie przed budową (`GET /v2/user/info/?fields=follower_count`). Dostępne przez `/followers`, narzędzie agenta-mentora `get_follower_growth`, i wpięte do cotygodniowego coachingu (realny sygnał czy CAŁE konto rośnie, nie tylko engagement postów). Wpięte w istniejący dzienny cron, zero nowego slotu. Poprzednio tego dnia: EPIC 11 zaprojektowany (planowanie), kampanie + wiedza o algorytmach platform, prawdziwe coachowanie, agent-mentor dodaje fanów/sprzedaże z rozmowy.
Stan builda: przechodzi (npm run build OK, npm test 359/359 OK w obu trybach APP_MODE, tsc --noEmit czyste poza dwoma znanymi błędami .mjs). Wdrożenie produkcyjne uruchomione (vercel --prod) po merge PR #82, status do potwierdzenia przez `postfly.pl/api/health` po zakończeniu.
Następne zadanie w kolejce: brak aktywnego. Dwie decyzje właściciela czekają: (1) konto procesora płatności (EPIC 5), (2) TASK-11.2.1 (zgoda na szersze uprawnienia OAuth) + TASK-11.2.2 (bezpośrednia weryfikacja wykonalności u TikToka) dla EPIC 11 Sprint 11.2. Pełne podsumowanie: postfly-plan-projektu.md, sekcja "Sprint 11.1 — śledzenie followersów, zaimplementowane".
Blokery/niedokończone wątki: brak blokerów technicznych. Blokery biznesowe/zewnętrzne: jak wyżej. Nadal nienaprawione: brak widoczności kosztu Claude, brak retencji `AgentConversationTurn`. **Cała funkcjonalność zbudowana w tej sesji wciąż NIE zweryfikowana przez prawdziwego bota** — w tym teraz też realne dane o followersach (appka musi zebrać dane min. przez tydzień, żeby `/followers` pokazał trend, nie tylko aktualną liczbę). PR-y od Dependabota czekają na przegląd.
Data/godzina ostatniej aktualizacji: 2026-09-14 08:45
