Ostatnio ukończone zadanie: **Dwie poprawki na prośbę właściciela (2026-09-16): 2FA "zapamiętaj to urządzenie" + brakujący `/schedule` w menu.** `/schedule` był w pełni funkcjonalny, ale nigdy nie miał wpisu w głównym sidebarze - naprawione, dodane jako "Harmonogram" obok "Kampanie". 2FA: kod przy każdym logowaniu okazał się realnym tarciem - nowy model `TwoFactorTrustedDevice` (osobne ciasteczko `postfly_2fa_remember`, 30 dni, httpOnly, NIGDY nie zastępuje hasła - tylko pomija drugi czynnik po tym jak hasło już zweryfikowano), checkbox na `/login` domyślnie zaznaczony. Wyłączenie 2FA czyści wszystkie zaufane urządzenia. Dodatkowo: przycisk "Zapomnij zapamiętane urządzenia" na `/account` (odwołanie zaufania bez wyłączania 2FA). Po raz pierwszy w tej sesji zweryfikowane pełnym, realnym przepływem w headless Playwright (`tests/e2e/login-2fa-remember-device.spec.ts`) - checkbox faktycznie działa w prawdziwej przeglądarce.
Stan builda: przechodzi (npm run build OK, npm test 537/537 OK w obu trybach APP_MODE, tsc --noEmit czyste). CI (świeże środowisko, brak lokalnych limitów) przeszło czysto za pierwszym razem, w tym nowy e2e test.
Następne zadanie w kolejce: brak aktywnego. Nadal otwarte z poprzednich tur: EPIC 10 Dashboard/GrowthPanel/media-library (średni priorytet audytu UX), Stripe Connect (implementacja, czeka na wznowienie działalności code94.pl), realny przegląd prawny przed komercjalizacją.
Blokery/niedokończone wątki: brak technicznych. Lokalny rate-limit logowania (10/15 min, magazyn w pamięci procesu dev) utrudnił wielokrotne ręczne testy e2e w tej turze - zdiagnozowane poprawnie jako artefakt środowiska testowego, nie appki; restart serwera dev resetuje.
Data/godzina ostatniej aktualizacji: 2026-09-16 05:40
